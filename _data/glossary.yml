format: "A DSL to define formatter configuration for an Xtext formatter."

check: "A DSL to define model constraints for Ecore-based AST of a DSL implemented with Xtext."

export: "A DSL to define externally visible objects of a DSL."

scope: "A DSL to define scope provider for a DSL."
